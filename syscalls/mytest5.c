#include <stdio.h>
#include <time.h>
#include <sys/socket.h>
#include <sys/types.h>

int main() {
	/* read / parse args */

	/* dns lookup (hostname to ip)*/
	
	/* infinite while */
	/* send_icmp */

	/* receive_icmp */

	/* use sendto system calls */

	return 0;
}